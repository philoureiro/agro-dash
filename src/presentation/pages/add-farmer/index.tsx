import { Typography } from '@mui/material';

import { FullSizeCentered } from '@components';

export const AddFarmer = () => {
  return (
    <>
      <meta name="title" content="Add farmer" />
      <FullSizeCentered>
        <Typography variant="h3">Add farmer</Typography>
      </FullSizeCentered>
    </>
  );
};
